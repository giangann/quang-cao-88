import { Box, Typography } from '@mui/material';

import { Banner } from './Banner';
import { Header } from './Header';

export const Home = () => {
  return (
    <Box>
      <Header />
      <Banner />
    </Box>
  );
};
